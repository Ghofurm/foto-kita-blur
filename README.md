# Foto Kita Blur — Peace Sign Camera Blur

> Deteksi gestur peace sign (✌️) via kamera, blur otomatis feed kamera.

## Quick Start

```bash
pip install -r requirements.txt
python src/main.py
```

Tekan `Q` untuk keluar.

## Build Executable

Lihat folder `build/` untuk script packaging per OS.
